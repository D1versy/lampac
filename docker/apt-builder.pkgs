# Пакеты стадии builder (нужны только чтобы скачать и распаковать SDK/рантайм).
# Один файл — один источник правды: отсюда их берёт и установка, и стадия сбора
# локального хранилища (assets-harvest), которую зовёт scripts\build-assets.ps1
# из репозитория оркестрации. Пустые строки и «#» игнорируются.
ca-certificates
curl
libicu76
xz-utils
